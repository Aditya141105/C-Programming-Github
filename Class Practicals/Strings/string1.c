#include <stdio.h>

int main() {

    char arr1[100], arr2[100];
    scanf("%s", arr1);
    // char input[100];  // Declare a character array to store the user's input

    // // Prompt the user to enter a line of text
    // printf("Enter a line of text: ");
    
    // // Read a line of text (including spaces) until a newline character is encountered
    // scanf("%[^\n]%*c", input);

    // // Print the input
    // printf("You entered: %s\n", input);

    return 0;
}
